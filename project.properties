project.name=Utilities
project.src=src
project.bin=bin
project.dist=dist

project.jar.path=${project.dist}
project.jar.name=${project.name}_${TODAY_DATE}.jar

project.resource.internal=resources
project.resource.external=

manifest.file=MANIFEST.MF
manifest.property.classpath=
manifest.property.builtby=Justin Palinkas
manifest.property.mainclass=

compiler.includes=**/*.java
compiler.excludes=
compiler.classpath=
compiler.args=
compiler.listfiles=true
compiler.source.version=1.7
compiler.target.version=1.7

#project.ref.output=../${project.name}/${project.dist}/libs
project.ref.output=

#project.ref.antfile=../Utilities/build.xml
project.ref.antfile=